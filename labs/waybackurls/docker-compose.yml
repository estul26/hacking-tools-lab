name: waybackurls-lab

services:
  target:
    build:
      context: .
      dockerfile: target.Dockerfile
    ports:
      - "127.0.0.1:18100:8080"
    networks:
      waybackurls_net:
        ipv4_address: 172.54.240.20

  scanner:
    build:
      context: .
      dockerfile: scanner.Dockerfile
    command: ["sleep", "infinity"]
    working_dir: /work
    volumes:
      - ./outputs:/work/outputs
      - ./fixtures:/work/fixtures:ro
      - ./scope:/work/scope:ro
    depends_on:
      - target
    networks:
      waybackurls_net:
        ipv4_address: 172.54.240.10

networks:
  waybackurls_net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.54.240.0/24
