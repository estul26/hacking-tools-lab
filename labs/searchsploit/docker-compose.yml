name: searchsploit-lab

services:
  scanner:
    build:
      context: .
      dockerfile: scanner.Dockerfile
    command: ["sleep", "infinity"]
    working_dir: /work
    volumes:
      - ./fixtures:/work/fixtures:ro
      - ./outputs:/work/outputs
