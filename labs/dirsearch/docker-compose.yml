name: dirsearch-lab

services:
  target:
    build:
      context: .
      dockerfile: target.Dockerfile
    ports:
      - "127.0.0.1:18090:8080"
    networks:
      dirsearch_net:
        ipv4_address: 172.44.140.20

  scanner:
    build:
      context: .
      dockerfile: scanner.Dockerfile
    command: ["sleep", "infinity"]
    working_dir: /work
    volumes:
      - ./outputs:/work/outputs
      - ./wordlists:/work/wordlists:ro
    depends_on:
      - target
    networks:
      dirsearch_net:
        ipv4_address: 172.44.140.10

networks:
  dirsearch_net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.44.140.0/24
